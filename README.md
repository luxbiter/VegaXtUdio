<img width="256" height="256" alt="No more U+ animation." src="https://github.com/user-attachments/assets/e19bafb7-310d-4c0b-bbea-7433b1865192" />


# VegaXtUdio - Vega X U+ Model Boot Animation Converter

This tool converts a normal Android boot animation folder made of PNG frames into the Pantech Vega X U+ model / EF14L Android 2.3.4 boot animation format.

The stock U+ `bootanimation` binary does not reliably play standard PNG boot animations. It expects `.lz` frames containing `ATC_RGB_AMD` compressed texture data, then packed into an uncompressed `bootanimation.zip`.

## Aseprite Requirements

Set these before exporting. These are not suggestions; the converter and the phone expect them.

- Canvas size: `480x800`
- Pixel aspect: square pixels
- Background: opaque black or fully drawn opaque pixels
- Color mode: `RGB Color`
- Frame size: every frame must stay exactly `480x800`
- Export format: PNG sequence
- Interlacing: off
- Scaling: `100%`
- Filename padding: use stable numeric order, for example `001.png`, `002.png`, `003.png`
- Animation rate: match `desc.txt`; the current example uses `20` fps

Recommended Aseprite export layout:

```text
BootAnim/
  desc.txt
  part0/
    000.png
    001.png
    002.png
  part1/
    000.png
```

Use `part0` for the intro animation and `part1` for the loop or final hold section. The folder names must match `desc.txt`.

## desc.txt

For Vega X use:

```text
480 800 20
p 1 0 part0
p 0 2 part1
```

The first line is `width height fps`.

The `p 1 0 part0` line plays `part0` once. The `p 0 2 part1` line loops `part1` with a 2-frame pause, matching the stock U+ animation.

## Convert

Run from this folder:

```sh
node scripts/convert_bootanim_lz.js --input BootAnim --output bootanimation.zip
```

This creates:

```text
BootAnim_lz/
  desc.txt
  part0/*.lz
  part1/*.lz

bootanimation.zip
```

The zip is written in `STORE` mode, not deflated. This matters on Android 2.3 boot animation loaders.

## Options

```text
--input <dir>     Source boot animation folder. Must contain desc.txt, part0, part1.
--workdir <dir>   Folder for generated .lz frames. Default: <input>_lz
--output <zip>    Output bootanimation.zip path. Default: bootanimation.zip
--copy-to <zip>   Also copy the output zip to another path.
--help            Show CLI help.
```

Example:

```sh
node scripts/convert_bootanim_lz.js --input MyAnim --workdir build/MyAnim_lz --output build/bootanimation.zip
```

## Install On Device

Common test path:

```sh
adb push bootanimation.zip /data/local/bootanimation.zip
adb shell chmod 644 /data/local/bootanimation.zip
adb shell bootanimation
```

System path:

```sh
adb remount
adb push bootanimation.zip /system/media/bootanimation.zip
adb shell chmod 644 /system/media/bootanimation.zip
```

## Notes

- This is for the Vega X U+ / EF14L Gingerbread boot animation binary.
- The converter uses a practical ATC RGB encoder tuned for boot logos and pixel animation. It is lossy by design.
- Very thin one-pixel text can soften because ATC compresses each `4x4` block. Larger lettering, high contrast shapes, and simple gradients survive best.
- If `bootanimation` segfaults, make sure the zip contains `.lz` frames, not `.png` frames, and that it was generated by this tool.

---

# VegaXtUdio - Vega X U+ 모델 부팅 애니메이션 변환기

이 도구는 일반 Android 부팅 애니메이션 폴더(PNG 프레임으로 구성)를 Pantech Vega X U+ 모델(EF14L) Android 2.3.4 부팅 애니메이션 형식으로 변환합니다.

기본 U+ `bootanimation` 바이너리는 일반적인 PNG 부팅 애니메이션을 안정적으로 재생하지 못합니다. 이 바이너리는 `ATC_RGB_AMD` 압축 텍스처 데이터가 포함된 `.lz` 형식의 프레임을 요구하며, 이를 압축하지 않은(STORE 모드) `bootanimation.zip`으로 패킹해야 합니다.

## Aseprite 요구 사항

내보내기 전에 반드시 아래 설정을 확인하세요. 변환기와 기기에서 필수적으로 요구하는 설정입니다.

* 캔버스 크기: `480x800`
* 픽셀 종횡비: 정사각형 픽셀
* 배경: 불투명한 검은색 또는 완전히 채워진 불투명 픽셀
* 색상 모드: `RGB Color`
* 프레임 크기: 모든 프레임은 정확히 `480x800`이어야 함
* 내보내기 형식: PNG 시퀀스
* 인터레이스(Interlacing): 끔
* 스케일링: `100%`
* 파일명 패딩: 일관된 숫자 순서 사용 (예: `001.png`, `002.png`, `003.png`)
* 애니메이션 속도: `desc.txt`와 일치시킬 것 (현재 예시는 `20` fps)

권장되는 Aseprite 내보내기 구조:

```text
BootAnim/
  desc.txt
  part0/
    000.png
    001.png
    002.png
  part1/
    000.png

```

`part0`은 시작(intro) 애니메이션, `part1`은 루프 또는 최종 유지 섹션으로 사용하세요. 폴더 이름은 `desc.txt`의 설정과 일치해야 합니다.

## desc.txt

Vega X의 경우 다음 설정을 사용하세요:

```text
480 800 20
p 1 0 part0
p 0 2 part1

```

첫 번째 줄은 `가로 세로 fps`를 의미합니다.

`p 1 0 part0`은 `part0`을 한 번 재생합니다. `p 0 2 part1`은 U+ 기본 애니메이션과 동일하게 2프레임 간격을 두고 `part1`을 루프 재생합니다.

## 변환

해당 폴더에서 다음 명령어를 실행하세요:

```sh
node scripts/convert_bootanim_lz.js --input BootAnim --output bootanimation.zip

```

이 명령어는 다음을 생성합니다:

```text
BootAnim_lz/
  desc.txt
  part0/*.lz
  part1/*.lz

bootanimation.zip

```

생성된 zip 파일은 압축(Deflate) 없이 `STORE` 모드로 작성됩니다. 이는 Android 2.3 부팅 애니메이션 로더에서 매우 중요합니다.

## 옵션

```text
--input <dir>     소스 부팅 애니메이션 폴더. desc.txt, part0, part1을 포함해야 함.
--workdir <dir>   .lz 프레임을 생성할 폴더. 기본값: <input>_lz
--output <zip>    출력될 bootanimation.zip 경로. 기본값: bootanimation.zip
--copy-to <zip>   출력된 zip 파일을 다른 경로로 복사.
--help            CLI 도움말 표시.

```

예시:

```sh
node scripts/convert_bootanim_lz.js --input MyAnim --workdir build/MyAnim_lz --output build/bootanimation.zip

```

## 기기 설치

테스트용 경로:

```sh
adb push bootanimation.zip /data/local/bootanimation.zip
adb shell chmod 644 /data/local/bootanimation.zip
adb shell bootanimation

```

시스템 적용 경로:

```sh
adb remount
adb push bootanimation.zip /system/media/bootanimation.zip
adb shell chmod 644 /system/media/bootanimation.zip

```

## 주의 사항

* 이 도구는 Vega X U+ / EF14L Gingerbread 부팅 애니메이션 바이너리 전용입니다.
* 변환기는 부팅 로고와 픽셀 애니메이션에 최적화된 ATC RGB 인코더를 사용합니다. 구조적으로 손실 압축이 발생합니다.
* 아주 얇은 1픽셀 텍스트는 ATC가 `4x4` 블록 단위로 압축하기 때문에 다소 흐려질 수 있습니다. 큰 글자, 대비가 높은 모양, 단순한 그라데이션이 가장 결과물이 좋습니다.
